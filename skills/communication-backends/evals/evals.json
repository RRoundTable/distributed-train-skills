{
  "skill_name": "communication-backends",
  "evals": [
    {
      "id": 1,
      "name": "ring-vs-tree-tradeoff",
      "prompt": "NCCL all-reduce에서 ring이랑 tree가 뭐가 달라? 언제 뭘 쓰는 거야?",
      "expected_output": "Ring: bandwidth-optimal 2(n-1)S/n but latency 2(n-1)*alpha linear in n. Tree: ~2*log2(n)*alpha latency, so wins for small/medium messages at large n. NCCL picks by size and world size.",
      "files": [],
      "assertions": [
        {
          "id": "ring-literal-bound",
          "description": "Gives ring's bandwidth term as the literal 2(n−1)S/n and states it is a lower bound that ring attains, not merely 'bandwidth-optimal'"
        },
        {
          "id": "ring-latency-alpha-form",
          "description": "Gives ring's latency as the literal 2(n−1)·α, not just O(N)"
        },
        {
          "id": "tree-latency-alpha-form",
          "description": "Gives the tree latency as the literal 2·log₂(n)·α, not just O(log N)"
        },
        {
          "id": "quantified-comparison",
          "description": "Quantifies the gap at a specific world size — e.g. at n=1024 ring pays 2046α versus the tree's 20α, about 100x"
        },
        {
          "id": "double-tree-mechanism",
          "description": "Explains the double-binary-tree construction: at least half the ranks in a binary tree are leaves, so a second tree is built with leaves and internal nodes swapped, making every rank both send and receive"
        },
        {
          "id": "rs-ag-identity-link",
          "description": "Connects the ring's two phases to the identity AllReduce = ReduceScatter ∘ AllGather"
        }
      ]
    },
    {
      "id": 2,
      "name": "busbw-vs-algbw",
      "prompt": "nccl-tests를 돌렸더니 algbw가 8 GB/s인데 우리 링크는 200 Gb/s짜리야. 이거 문제 있는 거야?",
      "expected_output": "Converts 200 Gb/s to 25 GB/s; explains algbw vs busbw and applies the 2(n-1)/n correction for all-reduce before comparing to link speed.",
      "files": [],
      "assertions": [
        {
          "id": "gb-to-gbyte",
          "description": "Converts 200 Gb/s to 25 GB/s by dividing by 8"
        },
        {
          "id": "allreduce-correction",
          "description": "Applies the literal all-reduce correction factor 2(n−1)/n and computes a numeric busbw from the given 8 GB/s"
        },
        {
          "id": "compare-busbw",
          "description": "States busbw is the figure to compare against link speed because algbw never reaches line rate by construction"
        },
        {
          "id": "large-message-asymptote",
          "description": "Says to read the large-message asymptote of a size sweep, since small sizes collapse due to the α term and that is not a fabric fault"
        },
        {
          "id": "crossover-interpretation",
          "description": "Notes the shape of busbw versus message size locates S* = α/β for that fabric, which is what sets bucket sizes and FSDP wrapping granularity"
        }
      ]
    },
    {
      "id": 3,
      "name": "timeout-means-a-rank-missing",
      "prompt": "Our job died with 'Watchdog caught collective operation timeout'. Should I just increase the NCCL timeout?",
      "expected_output": "No. A collective is a barrier, so a timeout means some rank never arrived. Ranks the causes by base rate starting with divergent control flow, and describes flight-recorder localization.",
      "files": [],
      "assertions": [
        {
          "id": "says-dont-raise-timeout",
          "description": "Advises against simply raising the timeout as the fix"
        },
        {
          "id": "barrier-insight",
          "description": "States that a collective is a barrier, so a timeout means some rank never arrived at it, not that the network is slow"
        },
        {
          "id": "reporting-rank-not-culprit",
          "description": "Notes the rank that reported the timeout is one that DID arrive, so the culprit is a silent rank"
        },
        {
          "id": "divergent-control-flow-first",
          "description": "Names divergent control flow (a rank skipping or taking a different branch, or an unequal dataloader length) as the most common cause"
        },
        {
          "id": "flight-recorder",
          "description": "Describes using the flight recorder / trace buffer and comparing collective sequence numbers across ranks to find which rank is behind"
        }
      ]
    },
    {
      "id": 4,
      "name": "bucket-size-derivation",
      "prompt": "Why does gradient bucketing help, and how should I pick the bucket size?",
      "expected_output": "k(alpha+beta*s) vs alpha+beta*k*s saves (k-1)*alpha; bucket must exceed S*=alpha/beta but stay small enough to leave overlap window; ~25MB default.",
      "files": [],
      "assertions": [
        {
          "id": "alpha-beta-model",
          "description": "Uses the α + β·S cost model explicitly"
        },
        {
          "id": "k-minus-1-alpha-saving",
          "description": "Shows the saving from bucketing is about (k−1)·α, the amortized per-message latency"
        },
        {
          "id": "crossover-point",
          "description": "Notes the bucket should be larger than the latency/bandwidth crossover S* = α/β"
        },
        {
          "id": "overlap-tradeoff",
          "description": "Notes a too-large bucket delays the start of communication and shrinks the overlap window with backward compute"
        }
      ]
    },
    {
      "id": 5,
      "name": "checkpoint-interval-math",
      "prompt": "체크포인트 저장에 120초 걸리고, job 단위 MTBF가 3시간이야. 얼마나 자주 저장해야 최적이야?",
      "expected_output": "T_opt = sqrt(2*C*MTBF) = sqrt(2*120*10800) ~ 1610s ~ 27 min, with ~15% overhead split evenly between write cost and lost work.",
      "files": [],
      "assertions": [
        {
          "id": "formula",
          "description": "States T_opt ≈ sqrt(2·C·MTBF)"
        },
        {
          "id": "computes-answer",
          "description": "Computes roughly 1610 seconds / about 27 minutes"
        },
        {
          "id": "two-terms-equal",
          "description": "Notes that at the optimum the write cost and the expected lost work are equal, and gives the ~15% total overhead"
        },
        {
          "id": "sqrt-c-scaling",
          "description": "Notes overhead scales as sqrt(2C/M), so halving checkpoint cost cuts total overhead by about 29% (1/√2) — the argument for async/sharded writes"
        },
        {
          "id": "mtbf-from-world-size",
          "description": "Connects job MTBF to world size via MTBF_job ≈ MTBF_1/n, or cites a real figure such as Llama 3's 419 unexpected interruptions in 54 days on 16K GPUs"
        }
      ]
    },
    {
      "id": 6,
      "name": "overlap-broken-diagnosis",
      "prompt": "My profile shows all the NCCL kernels running in one long block after the backward pass finishes, instead of overlapping. I'm using gradient accumulation. What's wrong?",
      "expected_output": "Missing no_sync() on the first m-1 micro-batches so an all-reduce fires every micro-batch; also lists host syncs, find_unused_parameters, and global grad clipping.",
      "files": [],
      "assertions": [
        {
          "id": "names-no-sync",
          "description": "Identifies missing no_sync() (or equivalent) under gradient accumulation as the likely cause"
        },
        {
          "id": "explains-m-fold-cost",
          "description": "Explains that without it an all-reduce fires every micro-batch, costing m times the expected communication"
        },
        {
          "id": "other-causes",
          "description": "Lists at least one other overlap breaker such as a host sync (.item()/.cpu()), find_unused_parameters=True, or global gradient clipping before the reduce"
        },
        {
          "id": "expected-good-pattern",
          "description": "Describes what a healthy profile looks like: NCCL kernels interleaved with backward compute, with only the last bucket exposed"
        }
      ]
    }
  ]
}
